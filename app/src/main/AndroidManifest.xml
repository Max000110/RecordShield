<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Overlay permission: draw transparent shield over Record button -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Foreground service & execution permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Telephony state observation -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />

    <!-- WakeLock for CPU watchdog verification -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Battery optimization exemption -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Boot and Direct Boot Receivers -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Post notification for foreground service (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- AlarmManager Watchdog -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecordShield"
        tools:targetApi="34">

        <!-- Main Dashboard Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- OEM Survival Assistant / Diagnostics Activity -->
        <activity
            android:name=".ui.DiagnosticsActivity"
            android:exported="false"
            android:label="OEM Survival Assistant"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Accessibility Service: detects in-call UI appearance and finds Record node bounds -->
        <service
            android:name=".service.RecordShieldAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Foreground Service: manages overlay window lifecycle -->
        <service
            android:name=".service.ShieldForegroundService"
            android:exported="true"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="call_ui_protection" />
        </service>

        <!-- Emergency Restart Receiver: Called if service is killed or process is removed from recents -->
        <receiver
            android:name=".service.EmergencyRestartReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.vivorecordshield.EMERGENCY_RESTART" />
            </intent-filter>
        </receiver>

        <!-- AlarmManager Watchdog Receiver -->
        <receiver
            android:name=".watchdog.AlarmWatchdogReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.vivorecordshield.ALARM_WATCHDOG_TICK" />
            </intent-filter>
        </receiver>

        <!-- Direct Boot Aware Boot Receiver -->
        <receiver
            android:name=".service.BootReceiver"
            android:directBootAware="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.USER_UNLOCKED" />
            </intent-filter>
        </receiver>

        <!-- System State Receiver (Power connected/disconnected, User Present heartbeat) -->
        <receiver
            android:name=".receiver.SystemStateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.ACTION_POWER_CONNECTED" />
                <action android:name="android.intent.action.ACTION_POWER_DISCONNECTED" />
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
